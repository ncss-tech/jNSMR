#' @section Package Options:
#' 
#' `jNSMR.JAR_SUFFIX` - can be used to select alternate JAR file from `inst/java` to add to Java classpath. It is assumed that the JAR file starts with the prefix `"newhall"` and has file name `".jar"`
#'  
#' @keywords internal
"_PACKAGE"

NULL
